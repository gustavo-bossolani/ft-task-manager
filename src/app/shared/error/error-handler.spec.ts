import { GenericErrorHandler } from './generic-error-handler';

describe('ErrorHandler', () => {
  it('should create an instance', () => {
    expect(new GenericErrorHandler()).toBeTruthy();
  });
});
